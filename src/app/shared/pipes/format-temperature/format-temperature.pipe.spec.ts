import { FormatTemperaturePipe } from './format-temperature.pipe';

describe('FormatTemperaturePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTemperaturePipe();
    expect(pipe).toBeTruthy();
  });
});
